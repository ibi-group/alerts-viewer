# Alerts Viewer

Relies on ATIS data, but components are generic enough to be able to handle other data sources.

TODO: Move data fetching into utils file, allow component to handle abstract Alerts data format. Second types file to indicate ATIS types vs. "our" standard